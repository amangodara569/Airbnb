when we enter data in form the browser and the web server will check to see that the data is in the correct format and within the constraints set by the application

# schema validation
    individual field me validation bhi lagana hai 
    otherwise complete details ke bina hi db me data save hone lagega

    use tool JOI FOR SCHEMA VALIDATION
    with that we have to define server side validation schema